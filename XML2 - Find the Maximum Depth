
maxdepth = 0

def depth(elem, level):
    global maxdepth
    
    # Increment level for children
    # We use (level + 1) because the root is passed as 0
    if level + 1 > maxdepth:
        maxdepth = level + 1
        
    for child in elem:
        depth(child, level + 1)
