def wrapper(f):
    def fun(l):
        formatted_numbers = []
        for num in l:
            clean_num = num[-10:]
           
            formatted_numbers.append(f"+91 {clean_num[:5]} {clean_num[5:]}")
      
        return f(sorted(formatted_numbers))
    return fun
